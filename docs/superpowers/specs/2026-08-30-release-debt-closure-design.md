# 发布收尾债务设计

## 目标

清除当前已经由验证命令确认的发布阻塞项，使主分支达到：后端关键回归测试通过、前端类型检查与生产构建通过、最新数据库迁移链可执行。

## 范围

### 后端同步契约

- 以当前向量数据库接口为准，核对 `delete_journal` 的参数签名。
- 修正过期的测试替身或真实调用方，保证云端向量删除失败仍返回可重试同步错误。
- 不弱化错误传播，不把云端索引失败重新改成静默成功。

### 前端类型安全

- 修复 `ProfilePanel` 登出后跳转 `/login` 的 typed-routes 类型错误。
- 保留 Next.js typed routes，不通过关闭检查或宽泛类型断言绕过。

### 数据库迁移

- 验证 Alembic 最新 head，覆盖待办归档/垃圾桶、项目归档/排序、集合用户隔离。
- 使用临时 PostgreSQL 不可用时，以迁移图检查、SQLite 可执行验证和既有 Neon bootstrap 路径共同验证。
- 生产 Neon 仍由用户本机通过已有 `DATABASE_URL` 执行 `scripts/bootstrap_neon.py`；不在应用请求路径自动迁移。

## 验收

- 后端关键测试全部通过，随后运行完整 Python 测试集并记录结果。
- 前端 Biome、`tsc --noEmit`、Next.js 生产构建通过。
- Alembic 只有一个 head，bootstrap 脚本可编译，迁移升级检查不出现结构错误。
- 只提交与上述问题直接相关的文件；不删除旧 worktree，不清理非阻塞弃用警告。
